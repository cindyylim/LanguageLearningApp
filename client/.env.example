# Local development — use CRA proxy (see package.json "proxy")
REACT_APP_API_URL=http://localhost:5000/api

# Production build (set on Render static site before build):
# REACT_APP_API_URL=https://languagelearningapp-server.onrender.com/api
